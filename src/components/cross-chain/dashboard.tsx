"use client";

import React from 'react';
import { CrossChainDashboard as FixedDashboard } from './dashboard-fixed';

// Simple redirect component to the fixed dashboard
export function CrossChainDashboard() {
  return <FixedDashboard />;
}
